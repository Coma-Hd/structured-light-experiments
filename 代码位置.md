# 代码位置说明

仓库里**具体实现代码**不在 `ceshi/*/0_*.ps1` 里。  
那些 PowerShell 文件只是**流程入口**（调参数、清目录、调用下面的 Python）。

## 1. 核心算法库（最重要）

路径：**[`src/`](src/)**

| 文件 | 作用 |
|------|------|
| `reconstruct.py` | 逐帧激光重建与融合（导轨 / 转台 / ChArUco） |
| `laser_center.py` | 蓝激光中心线提取 |
| `calib_intrinsic.py` / `calib_laser_plane.py` | 相机内参、激光平面标定 |
| `charuco.py` / `charuco_tracking.py` | 标定板检测与跟踪 |
| `geometry.py` | 射线、平面求交、坐标变换 |
| `postprocess.py` | 点云后处理 |
| `rail_poses.py` | 导轨位姿 |
| `turntable_motor.py` / `turntable_poses.py` | 转台电机与位姿 |
| `keyframe_roi.py` / `object_mask.py` | 关键帧 ROI、物体掩膜 |
| 其他 | 多视图合并、双面/四面拼接等 |

在线浏览：  
https://github.com/Coma-Hd/structured-light-experiments/tree/main/src

## 2. 可执行脚本（命令行入口）

路径：**[`scripts/`](scripts/)**

常见对应关系：

| 平台 | 脚本 |
|------|------|
| 导轨采集 | `continuous_rail_capture.py` |
| 转台采集 | `continuous_turntable_capture.py` |
| 转台轴标定 | `calibrate_turntable_axis.py` |
| 重建 | `3_reconstruct.py` → 调用 `src/reconstruct.py` |
| 后处理 | `4_postprocess.py` |
| 调试 | `debug_laser.py`、`draw_keyframe_roi.py` 等 |

在线浏览：  
https://github.com/Coma-Hd/structured-light-experiments/tree/main/scripts

## 3. 三类实验工作区（配置 + 流程壳）

路径：**[`ceshi/`](ceshi/)**

| 目录 | 内容 |
|------|------|
| `ceshi/导轨/` | `*.ps1`、`curve_scan.yaml`、标定、文档；调用根目录 `scripts/` |
| `ceshi/转台/` | 转台流程脚本与配置 |
| `ceshi/手动机械臂/` | 流程脚本 + **本目录内** `continuous_arm_capture.py`、`check_poses.py` |

机械臂有两份额外 Python 放在类别目录内：

- https://github.com/Coma-Hd/structured-light-experiments/blob/main/ceshi/%E6%89%8B%E5%8A%A8%E6%9C%BA%E6%A2%B0%E8%87%82/continuous_arm_capture.py
- https://github.com/Coma-Hd/structured-light-experiments/blob/main/ceshi/%E6%89%8B%E5%8A%A8%E6%9C%BA%E6%A2%B0%E8%87%82/check_poses.py

## 4. 调用关系（简图）

```text
ceshi/导轨/0_capture.ps1
        │
        ▼
scripts/continuous_rail_capture.py
        │
        ▼
src/laser_center.py / src/charuco*.py / ...
```

```text
ceshi/导轨/2_rebuild.ps1
        │
        ▼
scripts/3_reconstruct.py
        │
        ▼
src/reconstruct.py
```

## 5. 测试代码

路径：**[`tests/`](tests/)**（单元测试）
